//
#include <stdio.h>
#define INPUT(x) scanf("%d", &x);
//# define INPUT(x,y) scanf("%d%d",&x,&y);
#define OUTPUT(x) printf("%d\n", x);
int main()
{
    int a;
    INPUT(a)
    OUTPUT(a)
    return 0;
}